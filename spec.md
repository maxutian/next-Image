# Feature Spec: 为自开发聊天框增加新会话以及基于会话的context隔离/记忆功能

## 1. 背景

### 当前现状
- 项目名称：`IImage`
- 技术栈：`Next.js App Router`、`TypeScript`、`Tailwind CSS`、`Supabase`、`Gemini`
- 当前主流程：
  - 用户通过 `Supabase Auth` 登录
  - 通过 `generateImageAction` 执行文生图或图生图
  - 生成结果上传到 `Supabase Storage`
  - 结果和 prompt embedding 写入 `public.images`

### 问题描述
当前只有一个主会话，无法开启新会话，并且在同一个会话中无法读取之前的context
- 

### 为什么现在做
用户可以新开会话，并且可以基于之前的对话追问或者追加图片的修改
- 

## 2. 目标

### 业务目标
- 

### 用户目标
- 

### 成功标准
- 

## 3. 非目标

- 不修改与本 feature 无关的历史功能
- 不进行大规模 UI 重设计，除非该功能强依赖
- 不重构整套生成链路，除非是功能落地的必要条件

## 4. 目标用户与使用场景

### 目标用户
- 已登录用户
- 

### 典型场景
1. 
2. 
3. 

## 5. 用户故事
1. 作为使用者，我希望可以开启新会话，从而实现崭新的上下文，避免生成的不同场景的图片相互混淆.
2. 作为使用者，我希望可以在同一个会话中读取之前的上下文，从而实现对同一个图片的追加修改或者二次修改.

## 6. 范围

### In Scope
- 
- 
- 

### Out of Scope
- 
- 

## 7. 功能需求

### 核心需求
- `FR-1`：用户可以新增会话，重置每个新会话的上下文
- `FR-2`：切换历史会话，恢复之前的上下文
- `FR-3`：可以根据之前的上下文追问/二次编辑图片

### 交互需求
- `UX-1`：在页面左侧新增一个会话管理列表，可以切换不同会话
- `UX-2`：切换会话，右侧chat区域展示对应的历史消息记录

## 8. 技术约束

- 保持 `Next.js App Router` 现有架构
- 优先复用已有 `Server Actions`、认证与存储链路
- 新增环境变量必须写入 `.env.example`
- 如果涉及数据库结构变更，必须提供 `supabase/migrations/*`
- 如果涉及前端能力，需兼顾桌面端与移动端基本可用性

